{
  "buildCommand": "cd artifacts/portfolio && pnpm install --frozen-lockfile && pnpm run build",
  "outputDirectory": "artifacts/portfolio/dist/public",
  "installCommand": "pnpm install --frozen-lockfile"
}
